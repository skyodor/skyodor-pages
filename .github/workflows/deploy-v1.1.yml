name: Legacy versioned publisher (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic publishing is disabled. GitHub Pages deployment is handled by pages.yml."
